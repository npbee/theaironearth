<script lang="ts">
  import Icon from './Icon.svelte';
  import Logo from './Logo.svelte';
  import SiteListenLinks from './SiteListenLinks.svelte';

  export let size: 'default' | 'compact' = 'default';
</script>

<footer class="flex flex-col justify-between gap-4 text-muted w-full pt-16">
  <div class="flex flex-col md:flex-row gap-16 justify-between">
    <div class="flex gap-6 items-center w-full md:w-auto">
      {#if size === 'default'}
        <div class="w-16">
          <Logo />
        </div>
      {/if}
      <div class="flex gap-4 text-xs w-full justify-between sm:justify-start">
        <a href="https://instagram.com/npbeep">
          <Icon icon="instagram" />
          <span class="sr-only" />
        </a>
        <Icon icon="twitter" />
        <Icon icon="email" />
      </div>
    </div>
    <div class="grid grid-cols-1 gap-8 md:flex md:gap-8 flex-col sm:flex-row sm:items-center">
      <SiteListenLinks />
    </div>
  </div>
</footer>
